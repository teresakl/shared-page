# couple-calendar server
# 需要 Python >= 3.10（mcp>=2 的硬性要求；系统自带 3.9 装不上，建议 3.12）
fastapi
uvicorn
aiosqlite
python-multipart
mcp>=2  # mcp_server.py 用的是 2.0 的 lowlevel API（1.x 的装饰器写法已移除）
anyio     # mcp_server.py 顶层直接用到，同上
tzdata    # Windows 没有系统 IANA 时区库，供 zoneinfo 使用
