# 钥匙不进分享包。
# 上面那条带路径，只挡 Sources 里那一份；下面这条不带路径，
# 不管它被复制到哪一层都挡得住（_wip-network-1310 里就躺着一份一模一样的）
Sources/CalendarSecrets.swift
CalendarSecrets.swift

# 备份 / 施工中的目录都是 Sources 的整份拷贝，token 会被顺手复制进去，
# 只挡主文件挡不住，这几条必须有
_backup-*/
_wip-*/
_sticker-originals/
*.bak
*.bak.*
*.bak*

# Xcode 垃圾
build/
DerivedData/
*.xcuserstate
xcuserdata/
.DS_Store
